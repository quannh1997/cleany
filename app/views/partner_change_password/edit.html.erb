<% content_for :for_content do %>
  <!-- ##### Breadcrumb Area Start ##### -->
  <div class="vizew-breadcrumb" style="padding-top: 20px; padding-bottom: 15px;">
      <div class="container">
          <div class="row">
              <div class="col-12">
                  <nav aria-label="breadcrumb">
                      <ol class="breadcrumb">
                          <li class="breadcrumb-item"><a href="<%= root_path %>"><i class="fa fa-home" aria-hidden="true"></i> Home</a></li>
                          <li class="breadcrumb-item active" aria-current="page">ChangePassword</li>
                      </ol>
                  </nav>
              </div>
          </div>
      </div>
  </div>
  <!-- ##### Breadcrumb Area End ##### -->

  <!-- ##### Login Area Start ##### -->
  <div class="vizew-login-area section-padding-80" style="padding-top: 0">
      <div class="container">
          <div class="row justify-content-center">
              <div class="col-12 col-md-6">
                  <div class="login-content">
                      <!-- Section Title -->
                      <div class="section-heading">
                          <h4>Change your password</h4>
                          <div class="line"></div>
                      </div>

                      <%= form_for @user, url: partner_change_password_path(params[:id]) do |f| %>
                        <div class="form-group">
                          <%= f.password_field :old_password, placeholder: "Enter old password", class: "form-control", required: true %>
                        </div>
                        <div class="form-group">
                          <%= f.password_field :password, placeholder: "Enter new password", class: "form-control", required: true %>
                        </div>
                        <div class="form-group">
                          <%= f.password_field :password_confirmation, placeholder: "Confirm new Password", class: "form-control", required: true %>
                        </div>
                        <%= f.submit "Update Password", class: "btn vizew-btn w-100 mt-30" %>
                      <% end %>
                  </div>
              </div>
          </div>
      </div>
  </div>
  <!-- ##### Login Area End ##### -->
<% end %>