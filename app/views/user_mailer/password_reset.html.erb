<h1>Reset password</h1>
<p><%= 'Dear ' + @user.name %></p>
<p>You are receiving this email because we received a request to reset the <%= @user.class.to_s  %> account of `Cleany`. </p>
<p>If you do not do this please disregard this email.</p>
Click 
<% if  @user.class.to_s == 'Partner'%>
    <%= link_to "reset link", edit_partner_password_reset_url(id: @user.reset_token,
                                                      email: @user.email) %>
<% else %>
    <%= link_to "reset link", edit_password_reset_url(id: @user.reset_token,
                                                      email: @user.email) %>
<% end %>
 to reset your account